例子描述：
使用了两种native方法，一个是android库里的native方法，一个是第三方自定义库的native方法。

敏感信息是移动设备的手机号，有两种流向。
1.android库里的native方法：使用了android.util.Log里提供的isLoggable本地方法。如果“InCall”标签是可输出的，那就以日志的形式输出该标签对应的敏感信息（移动设备的手机号）。
2.第三方自定义库的native方法：将敏感信息封装到一个对象里，将带有敏感信息的对象传入到第三方库里的propagateData本地方法中，该方法获取对象中敏感信息，并将调用MainActivity中的java方法sendMessage，将敏感信息传入到sendMessage方法中，sendMessage方法将敏感信息以短信的形式明文发送到指定位置，造成敏感信息的泄漏。

该例子中是使用了日志的方法对敏感信息可进行明文查看，也可通过发送短信将敏感信息泄漏到指定位置，只是使用的泄漏手段不同。但都是通过使用了native方法泄漏敏感信息，尤其是第三方库的native方法，通过隐式地调用Java方法，造成信息泄漏。
